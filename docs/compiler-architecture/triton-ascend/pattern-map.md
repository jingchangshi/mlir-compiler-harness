# Pattern Map — triton-ascend

Generated by `repo-map` step 6b. HEAD `8ba4ac4ce` · indexer v21.

- **20 passes** with provenance chains; **124 passes** graph-confirmed zero patterns.
- Upstream Triton idioms parse cleanly with the generic engine: `populate*` populators,
  direct `patterns.add` in pass classes, `OpRewritePattern/OpConversionPattern` bases.

## Passes with pattern ownership

| pass | patterns (ownership) |
|---|---|
| `pass:convert-triton-to-ascend` | `ConvertArithBinaryOp` (direct); `ConvertArithCast` (direct); `ConvertArithCmpOp` (direct); `ConvertArithSelect` (direct); `ConvertMathUnaryOp` (direct); `ConvertTritonBroadcast` (direct); `ConvertTritonDot` (direct); `ConvertTritonLoad` (direct); `ConvertTritonReduce` (direct); `ConvertTritonStore` (direct); `ConvertTritonTrans` (direct); `EraseDeadTritonAddrOps` (direct) |
| `pass:convert-triton-to-tritongpu` | `—` via `populateArithPatternsAndLegality` (confirmed); `CFBranchPattern,CFCondBranchPattern` via `populateCFPatterns` (confirmed); `AbsFOp,AbsIOp,CeilOp,CosOp,ErfOp,Exp2Op,ExpOp,FloorOp,FmaOp,Log2Op,LogOp,RsqrtOp,SinOp,SqrtOp` via `populateMathPatternsAndLegality` (confirmed); `SCFConditionPattern,SCFForPattern,SCFIfPattern,SCFWhilePattern,YieldOp` via `populateSCFPatterns` (confirmed); `—` via `populateTritonPatterns` (confirmed) |
| `pass:gluon-canonicalize` | `ForOpDeadArgElimination` via `populateForOpDeadArgumentElimination` (confirmed) |
| `pass:gluon-slimplify-control-flow` | `ForOpDeadArgElimination` via `populateForOpDeadArgumentElimination` (confirmed) |
| `pass:merge-concat-load-buffer` | `MergeConcatLoadBufferPattern` (direct) |
| `pass:triton-nvidia-mma-lowering` | `MergeCommitIntoMMA` (direct); `SyncMMALowering` (direct); `TCGen5MMAScaleSharedToTmemConversion` (direct) |
| `pass:triton-nvidia-tma-lowering` | `TMACreateDescLowering` (direct); `TMAGatherLowering` (direct); `TMALoadLowering` (direct); `TMAReduceLowering` (direct); `TMAScatterLowering` (direct); `TMAStoreLowering` (direct) |
| `pass:triton-to-annotation` | `TritonAnnotationConversionPattern` (direct) |
| `pass:triton-to-hfusion` | `TritonConv1dToHFusionConversion` (direct); `TritonConv2dToHFusionConversion` (direct); `TritonFpToFpToHFusionConversion` (direct); `TritonHistogramToHFusionConversion` (direct); `TritonModToHFusionConversion` (direct) |
| `pass:triton-to-hivm` | `TritonCustomOpToHIVMSyncOpConversion` (direct) |
| `pass:triton-to-linalg` | `AtomicCASOp,AtomicMaxMinCanonicalizer,AtomicRMWOp,BitcastCanonicalizer,FoldScanOffsetAddPtrChain,FpToFpCanonicalizer,LoadOp,ReduceSingleCanonicalizer,ScalarAtomicCASCanonicalizer,ScalarAtomicRMWCanonicalizer,ScalarStoreCanonicalizer,SelectCanonicalizer,StoreOp` via `populateTritonToLinalgCanonicalizationPatterns` (confirmed); `AddPtrConverter,AdvanceConverter,ArgMaxConverter,ArgMinConverter,AtomicCASConverter,AtomicRMWConverter,BitcastConverter,BroadcastConverter,CatConverter,ClampFConverter,CustomMacroOp,CustomOp,DenseConstantConverter,DeviceAssertConverter,DevicePrintConverter,DotConverter,DotScaledConverter,ExpandDimsConverter,ExternElementwiseClOpConverter,FlipOpConverter,ForOp,GatherConverter,GatherOutToUbConverter,GetNumProgramsConverter,GetProgramIDConverter,HistogramConverter,IfConverter,IndexPutConverter,IndexSelectSimdConverter,IndirectLoadConverter,IndirectStoreConverter,IntToPtrConverter,JoinConverter,LoadConverter,MakeRangeConverter,MakeTensorPtrConverter,MapElementwiseDecomposeConverter,MatmulConverter,MetaUseEraser,PointerSelectConverter,PreciseDivConverter,PtrToIntConverter,ReduceConverter,ReshapeConverter,ScanConverter,ScatterUbToOutConverter,SortOpConverter,SplatConverter,SplitConverter,StoreConverter,StrideLoadConverter,StrideStoreConverter,TransposeConverter,TritonMulhiuiConverter,TritonPreciseSqrtConverter,UnsplatConverter,WhileOp,YieldConverter` via `populateTritonToLinalgConversionPatterns` (confirmed) |
| `pass:triton-to-llvm` | `ElementwiseInlineAsmOpConversion` (direct) |
| `pass:triton-to-structured` | `AddPtrSplatConverter,CmpConverter,LoadBroadcastConverter,PromotePointerIterArgsPattern,SimplifyTensorIterArgsPattern,ZeroStrideMakeTensorPtrConverter` via `populateTritonToStructuredCanonicalizationPatterns` (confirmed); `LoadConverter,StoreConverter` via `populateTritonToStructuredPatterns` (confirmed) |
| `pass:tritongpu-accelerate-matmul` | `BlockedToMMA` (direct); `BlockedToMMAv5` (direct); `ScaledBlockedToMMA` (direct); `ScaledBlockedToMMAv5` (direct); `DecomposeScaledBlocked` via `populateDecomposeScaledBlockedPatterns` (confirmed) |
| `pass:tritongpu-coalesce-async-copy` | `ClipAsyncCopySizePerThread` (direct) |
| `pass:tritongpu-hoist-tmem-alloc` | `CombineTMEMLoadAndStore` (direct); `CombineTMEMStoreAndAlloc` (direct); `CombineTMEMStoreAndSelect` (direct); `HoistTMEMAllocOutOfIf` (direct); `RemoveUnusedTMEMLoad` (direct); `RotateTMEMLoadInLoop` (direct); `RotateTMEMStoreInLoop` (direct); `SinkTMEMLoad` (direct); `TMEMLoadForwarding` (direct) |
| `pass:tritongpu-optimize-accumulator-init` | `TMEMAllocWithUnusedInit` (direct) |
| `pass:tritongpu-optimize-dot-operands` | `FuseTransMMAV3Plus` (direct); `ReshapeMemDesc` (direct); `SwizzleShmemConvert` (direct); `UseShmemForScales` (direct) |
| `pass:tritongpu-promote-lhs-to-tmem` | `LHSToTMem` (direct) |
| `pass:tritongpu-remove-layout-conversions` | `ForOpDeadArgElimination` via `populateForOpDeadArgumentElimination` (confirmed) |

Per-hop evidence: `repo pattern-owner <Pattern>`.
